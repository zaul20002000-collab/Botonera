All of the app lives in the HTML file. To be able to play remote sounds, the app needs to be running into a web server, in this case serv.py. 
So to start the included server, be sure to use: $ python serv.py

---
Once running, you can import export the app configuration from the gotonera_config.json
